#Notas:
Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     La tabla de multiplicar saldrá en base a este valor
                                                             [number] [required]
  -l, --listar   Permite decidir si se muestra la tabla de multiplicar por
                 consola                              [boolean] [default: false]
  -h, --hasta    Máximo valor de la lista en la tabla de multiplicar
                                                          [number] [default: 10]
```